export interface CalculateMatchDTO {
    userAId: string;
    userBId: string;
    score: number;
}

export interface CalculateMatchResponseDTO {
    userAId: string;
    userBId: string;
    score: number;
}
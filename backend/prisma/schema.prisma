generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model User {
  id           String @id @default(cuid())
  spotifyId    String @unique
  displayName  String
  email        String @unique
  accessToken  String
  refreshToken String
}
